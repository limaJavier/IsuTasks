# Angular

## Create an angular project

```bash
$ ng new <project-name>
```

## Create a new component

```bash
$ ng generate component <component-name>
```

Notice that the `component-name` argument represents the path of the component's directory. For instance:

```bash
$ ng generate component features/home/registerComponent
```

is gonna create the register component in the relative directory `features/home`.
